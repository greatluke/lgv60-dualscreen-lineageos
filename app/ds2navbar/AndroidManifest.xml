<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lge.ds2.navbar"
    android:versionCode="1"
    android:versionName="0.1">

    <uses-sdk android:minSdkVersion="29" android:targetSdkVersion="34" />

    <application
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_menu_directions"
        android:allowBackup="false">

        <!-- An AccessibilityService because it is the only way to do this without privilege:
             it may add windows (a bare root app_process may not; WindowManager rejects it with
             "Unknown pid/uid"), and performGlobalAction() delivers back/home/recents without
             root, a signature permission, or an SELinux policy change. -->
        <service
            android:name=".NavBarAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
